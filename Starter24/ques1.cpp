#include <bits/stdc++.h>

using namespace std;

int main()
{

    int n;
    cin >> n;
    string s;
    cin >> s;

    vector<int> cost(n + 1);
    for (int i = 1; i <= n; i++)
    {
        cin >> cost[i];
    }

    

    return 0;
}